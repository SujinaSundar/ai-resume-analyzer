apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "resume-analyzer-chart.fullname" . }}-test-connection"
  labels:
    {{- include "resume-analyzer-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "resume-analyzer-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
